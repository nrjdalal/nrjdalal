# ~/.config/ghostty/config — read by cmux for its terminal surfaces.
# Reload after editing: cmux reload-config

# ~ appearance
theme = GitHub Dark Default
font-size = 12

# ~ transparency (native macOS Tahoe glass material)
background-opacity = 0.95
background-blur = macos-glass-regular
